let state = {
    Title: "",
    Content: "",
}
Const Reducer = (State, Action){
    switch (Action.Type) {
        case 'update.title':
            return {
                title: document.getElementById(input).value ?
        }
        case 'UpdateContent':
            return {
                Content: state.content + document.getElementById(input) ?
        }
    }
    Default:
    Return State
}