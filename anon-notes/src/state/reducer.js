import {DATA_STATUS_HANDLER} from './action'
const initialState = {
    notes:''

}


export const reducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state;
    }


}